# Server
PORT=5000
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wealthmap
DB_USER=
DB_PASSWORD=

# Auth — generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# Claude API (needed only for the AI smell-test feature)
ANTHROPIC_API_KEY=